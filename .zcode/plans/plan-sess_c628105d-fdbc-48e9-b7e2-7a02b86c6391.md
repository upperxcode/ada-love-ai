Goal: Persist every app-level configuration entity exclusively in the SQLite DB. No JSON files (ada_config.json or config.json) should be used for runtime storage. Persist: Workspaces (all fields), Workers, Agents, Skills, Tools, SpecWizards, MCP servers, Providers/Models.

Scope: Update backend persistence to write/read full WorkspaceConfig fields and related entities to DB, and ensure engine.Load/Save flows no longer rely on SetGlobalConfig for these items. Keep SetGlobalConfig for small app state (active_workspace_index/path) only if user wants those normalized too (ask). We'll persist MCPs via mcps table and providers via SaveProviderFull which already exists.

Planned changes (detailed):
1) DB schema
  - Add columns to workspaces table: summary TEXT, enabled BOOL DEFAULT 1, max_prompt_send INTEGER DEFAULT 0, commit_changes BOOL DEFAULT 0, max_context_length INTEGER DEFAULT 0, embedding_model TEXT DEFAULT '', embedding_provider TEXT DEFAULT ''
  - Ensure mcps, providers, skills tables exist (they already do)
  - Ensure workspace_tools and workspace_skills junction tables exist (they already do)

2) backend/db.go
  - Update SaveWorkspace(ws) to INSERT/REPLACE the new columns.
  - Update GetWorkspaces() and GetWorkspaceByPath() SELECT to include and scan the new columns and populate WorkspaceConfig fields: Summary, Enabled, MaxPromptSend, CommitChanges, MaxContextLength, EmbeddingModel, EmbeddingProvider, Skills/Tools will be loaded via existing junction helpers.
  - Add calls in SaveWorkspace or elsewhere to persist junctions: SetWorkspaceSkills and SetWorkspaceTools. Prefer to let Engine handle resolution of skill names→IDs and pass IDs; SetWorkspaceTools accepts names so we can call it with ws.Tools.

3) backend/engine.go
  - Modify SaveAdaConfig to, after SaveWorkspace and resolving worker/agent ids: also resolve skill names to IDs and call SetWorkspaceSkills(id, skillIDs); call SetWorkspaceTools(id, ws.Tools) as well.
  - Replace calls that store MCPs/providers/models in config key-value JSON with SaveMCP/SaveProviderFull where appropriate. Persist MCPServers using SaveMCP (which writes normalized mcps table) instead of SetGlobalConfig.
  - Replace any remaining SetGlobalConfig calls for embedding_model/image_model/spec_model/spec_tools/tool_profiles/mcp_servers with normalized DB writes. Keep SetGlobalConfig only for truly arbitrary key/value states if desired.

4) Loading paths
  - Ensure NewEngine or engine init loads MCPs via GetMCPs (normalized) and loads workspace skills/tools from junction tables when building AdaConfig.Workspaces (GetWorkspaces currently loads WorkerNames, Agents, Folders, Knowledge — extend it to also populate Skills and Tools using existing DB helper functions).
  - Ensure providers and provider models continue to be loaded with GetProvidersFull.

5) Migration
  - Add ALTER TABLE statements in db.init() to add new columns if missing (safe, ignore errors). Add a DB migration record in schema_migrations (we already created schema_migrations earlier) to track these structural migrations.
  - Migrate any data currently in config.json / ada_config.json by reading values (migration step executed once) and moving them to normalized tables. This will run during init() and record migration version.

6) Tests & verification
  - Run go build to ensure compilation
  - Add minimal unit test(s) or a small main-run (if repo lacks test harness) that writes an AdaConfig with all fields and calls SaveAdaConfig, then reloads Engine to confirm GetWorkspaces etc. returns full fields.

Trade-offs and notes:
- I will reuse existing helper functions (SaveProviderFull, SaveSpecWizard, SaveWorker, SaveAgent, SetWorkspaceWorkers/Agents/Skills/Tools/Folders/Knowledge) to minimize duplication.
- Skill content currently exists on disk; we'll keep that (skills' files) but also use skills table as authoritative for installed/available skills. Saving workspace skills will map names→ids and populate workspace_skills. If the skill does not exist in DB but exists on disk, create a DB row via SaveSkill.
- For MCPs and Providers we will prefer normalized tables and not store them in SetGlobalConfig / config table.
- Because the codebase uses SetGlobalConfig in many places, this refactor will be somewhat invasive. We'll implement incrementally: first ensure workspace fields + skills/tools persist; next pass normalize MCPs/providers etc.

Implementation plan steps (to be executed after user approval):
A. (db.go) Add ALTER TABLE columns for workspaces (safe), update SaveWorkspace and GetWorkspaces/GetWorkspaceByPath to persist/scan new fields, and ensure SaveWorkspace triggers junction persistence or let Engine do it.
B. (engine.go) Update SaveAdaConfig to call SetWorkspaceSkills and SetWorkspaceTools after resolving names→ids, add provider/mcp normalization calls to SaveMCP/SaveProviderFull.
C. (db.init) Add migrations to move data from config table (embedding_model, spec_model, mcp_servers, tool_profiles) into normalized tables; record migration versions.
D. Update NewEngine loader to use normalized getters (GetMCPs, GetWorkspaces with skills/tools populated) and remove fallback reliance on ada_config.json after migration.
E. Run build and run basic integration sanity check: SaveAdaConfig, reload Engine, compare state.

Questions for the user (required before implementation):
1) Should I migrate all uses of SetGlobalConfig to normalized tables in this change, or do you prefer an incremental approach (first focus on workspace/skills/tools, then providers/mcps)?
2) Do you want SetGlobalConfig to be fully retired or kept for some UI-only ephemeral settings (active_workspace_index/path)?
3) Confirm mapping decision: commit_changes in WorkspaceConfig should map to a new workspaces.commit_changes column (instead of reusing existing workspaces."commit").

If you approve the plan and answer the 3 questions, I will implement the changes and run build verification.

